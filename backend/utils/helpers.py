"""
utils/helpers.py — Reusable utility functions shared across services/routers.

Empty for now at the skeleton stage. Per the project rule "reusable
functions go here", this is where things like timestamp formatting,
cooldown-window math, or response-shaping helpers will land as later
sections need them — keeping that logic out of services/routers directly.
"""
